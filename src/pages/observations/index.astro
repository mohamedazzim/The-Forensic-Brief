---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Card from '../../components/Card.astro';
import StructuredData from '../../components/StructuredData.astro';
import { getCollection } from 'astro:content';

const observations = await getCollection('observations', ({ data }) => data.status === 'published');
const sortedObservations = observations.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<BaseLayout title="Observations" description="Preliminary field notes and lab notebook entries on AI system behavior.">
  <StructuredData
    type="collection"
    data={{
      name: 'Observations',
      description: 'Preliminary field notes and lab notebook entries on AI system behavior.',
      url: 'https://theforensicbrief.com/observations/',
    }}
  />
  <section class="content-section">
    <div class="section-header">
      <h1 class="section-title">Observations</h1>
      <p class="section-description">Preliminary field notes and lab notebook entries on AI system behavior.</p>
    </div>
    <hr class="section-rule">

    {sortedObservations.length > 0 ? (
      <div class="card-grid">
        {sortedObservations.map((observation) => (
          <Card
            title={observation.data.title}
            excerpt={observation.data.summary}
            date={observation.data.date.toISOString().split('T')[0]}
            category={observation.data.observationStatus}
            href={`/observations/${observation.id.replace(/\.(md|mdx)$/, '')}/`}
            linkText="Read observation"
          />
        ))}
      </div>
    ) : (
      <p class="empty-state">No observations published yet.</p>
    )}
  </section>
</BaseLayout>

<style>
  .content-section {
    margin-bottom: 72px;
  }

  .section-header {
    margin-bottom: 8px;
  }

  .section-title {
    font-family: 'Playfair Display', Georgia, serif;
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--text-primary);
    line-height: 1.25;
  }

  .section-description {
    font-size: 0.9rem;
    color: var(--text-muted);
    margin-top: 6px;
    max-width: 560px;
  }

  .section-rule {
    width: 100%;
    height: 1px;
    background: var(--border-light);
    border: none;
    margin: 20px 0 28px;
  }

  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 20px;
  }

  .empty-state {
    text-align: center;
    padding: 60px 0;
    color: var(--text-muted);
    font-style: italic;
  }
</style>
